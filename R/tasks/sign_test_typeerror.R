# 执行显著性检验，并记录其第一类和第二类错误。

sign_test_typeerror.task_config_validation <- function(task_config) {
  # 验证显著性检验任务配置的正确性。
  
} 

sign_test_typeerror.perform_task <- function(task_config) {
  # 执行该显著性检验任务。
  
}